<UserControl x:Class="CvnetClient.Views.CvnetBtListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 Background="Transparent"
             >
	<!--<Button Content="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" 
            Click="BtList_Click" 
			VerticalAlignment="Stretch"
			HorizontalAlignment="Stretch" 
            Style="{StaticResource ComboLabelBtn}"/>-->

	<Button x:Name="MyButton"
            Content="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" 
            Click="BtList_Click" 
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch" 
            Style="{StaticResource ComboLabelBtn}"/>

</UserControl>