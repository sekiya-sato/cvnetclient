<UserControl x:Class="CvnetClient.Views.ListFlexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CvnetClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
			 Height="Auto" Width="Auto"
			 Name="listflexview">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/> 
			<RowDefinition Height="*"/> 
		</Grid.RowDefinitions>

		<Button Grid.Row="0" 
				Content="＋ 行追加" Click="AddRow_Click" 
				Width="80" 
				Margin="5"
				HorizontalAlignment="Left"/>
		<DataGrid Grid.Row="1"
				  ItemsSource="{Binding DataSource.ListFlexSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
				  AutoGenerateColumns="False" 
				  CanUserAddRows="False" 
				  HeadersVisibility="Column"
				  Margin="5" 
				  Style="{DynamicResource WinGridData}"
				  ColumnWidth="*">
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </DataGrid.CellStyle>
            
            <DataGrid.Columns>
				<!--行-->
                <DataGridTextColumn Header="行" Binding="{Binding No}" Width="*">
                <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>

                        </Style>
                        </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

				<!-- 検索項目 -->
				<DataGridTemplateColumn Header="検索項目" Width="3*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<ComboBox ItemsSource="{Binding DataSource.ListFlexVar.Unit_list, RelativeSource={RelativeSource AncestorType=UserControl}}"
									  DisplayMemberPath="Value"
									  SelectedValuePath="Value"
								      SelectedValue="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
									  SelectionChanged="ComboBox_SelectionChanged"> 
							</ComboBox>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

				<!-- 選択項目のコード(FROM) -->
				<DataGridTextColumn Header="選択項目のコード(FROM)" Binding="{Binding FromValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>

				<!-- 一覧 (FROM) -->
				<DataGridTemplateColumn Header="一覧" Width="0.5*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
                            <Button Content="一覧" Margin="0" IsEnabled="{Binding IsListFromEnabled}"  Click="ListFrom_Click" Style="{StaticResource FlexButton}"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

				<!-- 選択項目のコード(TO) -->
				<DataGridTextColumn Header="選択項目のコード(TO)" Binding="{Binding ToValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>

				<!-- 一覧 (TO) -->
				<DataGridTemplateColumn Header="一覧" Width="0.5*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
                            <Button Content="一覧" IsEnabled="{Binding IsListToEnabled}" Click="ListTo_Click" Style="{StaticResource FlexButton}">
                            </Button>
                        </DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

				<!-- 削除ボタン -->
				<DataGridTemplateColumn Header="削除" Width="*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
                            <Button Style="{DynamicResource DeleteFlexBtn}"
									Click="DeleteRow_Click"/>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

			</DataGrid.Columns>
		</DataGrid> 
	</Grid>
</UserControl>
