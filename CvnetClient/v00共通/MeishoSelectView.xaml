<Window
    x:Class="CvnetClient.Views.MeishoSelectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="clr-namespace:CvnetClient.ViewModels"
    Title="選択してください"
    Width="500"
    Height="350"
    FontFamily="Meiryo UI"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">
	<Window.DataContext>
		<vm:MeishoSelectViewModel />
	</Window.DataContext>
	<Grid Margin="4">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="40" />
		</Grid.RowDefinitions>
		<DataGrid
            Grid.Row="0"
            ItemsSource="{Binding List}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
            AutoGenerateColumns="False"
            HeadersVisibility="Column"
            IsReadOnly="True"
            SelectionMode="Single"
			RowHeight="24"
            Margin="0,0,0,4">
			<DataGrid.Columns>
				<DataGridTextColumn Header="コード" Binding="{Binding MeishoCd}" Width="100"/>
				<DataGridTextColumn Header="{Binding TitleName}" Binding="{Binding Meisho}" Width="*"/>
			</DataGrid.Columns>
		</DataGrid>
		<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,0">
			<Button Width="50" Margin="2" Content="&gt;&gt;" Command="{Binding NextCommand}" />
			<TextBox Width="120" Margin="2" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
			<Button Width="100" Margin="2" Content="名称検索" Command="{Binding SearchCommand}" />
			<Button Width="120" Margin="2" Content="選択実行(Enter)" Command="{Binding SelectCommand}" IsDefault="True" />
			<Button Width="79" Margin="2" Content="戻る(ESC)" Command="{Binding ExitCommand}" IsCancel="True" />
		</StackPanel>
	</Grid>
</Window>