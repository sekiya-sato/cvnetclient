<Window x:Class="CvnetClient.Views.SubDlg80GphABC2aView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CvnetClient.Views"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        mc:Ignorable="d"
        Title="SubDlg80GphABC2aView" Height="450" Width="800">
    <Window.DataContext>
        <vm:SubDlg80GphABC2aViewModel/>
    </Window.DataContext>
    <Grid>
        <oxy:PlotView Model="{Binding AbcModel}"
              Controller="{Binding Controller}"
              VerticalAlignment="Stretch"
              HorizontalAlignment="Stretch"
              MouseDoubleClick="PlotView_MouseDoubleClick"/>

        <TextBlock Text="{Binding SelectedInfo}" 
                   VerticalAlignment="Bottom" 
                   Margin="10" FontSize="16" />
    </Grid>

</Window>
