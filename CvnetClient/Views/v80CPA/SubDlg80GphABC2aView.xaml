<Window x:Class="CvnetClient.Views.SubDlg80GphABC2aView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CvnetClient.Views"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        mc:Ignorable="d"
        Title="SubDlg80GphABC2aView" Style="{StaticResource MasterWindowStyle}">
    <Window.DataContext>
        <vm:SubDlg80GphABC2aViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="3.5*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="3.5*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="選択行" VerticalAlignment="Center" Margin="0,8"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />

                    <Label Grid.Row="0" Grid.Column="2" Content="ブランド" VerticalAlignment="Center" Margin="0,8"/>
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="4*"/>
                        <ColumnDefinition Width="0.5*"/>
                        <ColumnDefinition Width="4*"/>
                    </Grid.ColumnDefinitions>
                    <Label  Grid.Column="0" Content="伝票No" VerticalAlignment="Center" Margin="0,8"/>
                    <TextBox  Grid.Column="1"  Margin="0,8" Text="{Binding EditSearch.DenpyoNo1}" />
                    <Label  Grid.Column="2" Content="~" VerticalAlignment="Center" Margin="0,8" HorizontalAlignment="Center"/>
                    <TextBox  Grid.Column="3"  Margin="0,8" Text="{Binding EditSearch.DenpyoNo2}"/>
                </Grid>
            </Grid>
            <DataGrid Grid.Row="1"
          ItemsSource="{Binding AbcTable}"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          SelectionMode="Single"
          Margin="8,10,8,10">

                <DataGrid.Resources>
                    <!-- Row style ikut Category -->
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Category}" Value="A">
                                <Setter Property="Background" Value="Orange"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Category}" Value="B">
                                <Setter Property="Background" Value="MediumPurple"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Category}" Value="C">
                                <Setter Property="Background" Value="LightGreen"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Width="*" Binding="{Binding ブランドCD}" Header="CD"/>
                    <DataGridTextColumn Width="*" Binding="{Binding ブランド名}" Header="ブランド名"/>
                    <DataGridTextColumn Width="*" Binding="{Binding アイテムCD}" Header="CD"/>
                    <DataGridTextColumn Width="*" Binding="{Binding アイテム名}" Header="アイテム名"/>
                    <DataGridTextColumn Width="*" Binding="{Binding 数量}" Header="数量"/>
                    <DataGridTextColumn Width="*" Binding="{Binding 金額}" Header="金額"/>
                    <DataGridTextColumn Width="*" Binding="{Binding 構成比}" Header="構成比"/>
                    <DataGridTextColumn Width="*" Binding="{Binding 累計比}" Header="累計比"/>
                </DataGrid.Columns>
            </DataGrid>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="8*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="選択行" VerticalAlignment="Center" Margin="0,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />

                <Label Grid.Row="1" Grid.Column="0" Content="ブランド" VerticalAlignment="Center" Margin="0,8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />

                <Label Grid.Row="2" Grid.Column="0" Content="アイテム" VerticalAlignment="Center" Margin="0,8"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />

                <Label Grid.Row="3" Grid.Column="0" Content="合計" VerticalAlignment="Center" Margin="0,8"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />

                <Label Grid.Row="4" Grid.Column="0" Content="構成比" VerticalAlignment="Center" Margin="0,8"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />

                <Label Grid.Row="5" Grid.Column="0" Content="累計比" VerticalAlignment="Center" Margin="0,8"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedInfo}" VerticalAlignment="Center" Margin="10" />
            </Grid>
            
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="9*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <oxy:PlotView Grid.Row="0" Model="{Binding AbcModel}"
              Controller="{Binding Controller}"
              VerticalAlignment="Stretch"
              HorizontalAlignment="Stretch"
              MouseDoubleClick="PlotView_MouseDoubleClick"/>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="CSV出力" Style="{DynamicResource PrintIconBtn}" Margin="8"/>
                <Button Style="{DynamicResource PrintIconBtn}" Margin="8"/>
                <Button  Margin="8" Style="{DynamicResource BackBtn}" Command="{Binding ExitCommand}" IsCancel="True" HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>
        
        
    </Grid>

</Window>
