<Window x:Class="CvnetClient.Views.SubDlg01WeekView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
		xmlns:uc="clr-namespace:CvnetClient.Views"
        mc:Ignorable="d"
		Title="{Binding Title}"
		Style="{StaticResource MasterWindowStyle}">
	<Window.DataContext>
		<vm:SubDlg01WeekViewModel />
	</Window.DataContext>
	<behaviors:Interaction.Triggers>
		<behaviors:EventTrigger EventName="ContentRendered">
			<behaviors:InvokeCommandAction Command="{Binding InitCommand}" />
		</behaviors:EventTrigger>
	</behaviors:Interaction.Triggers>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="30" />
		</Grid.RowDefinitions>

		<Grid Grid.Row="0" Margin="5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="2*" />
				<ColumnDefinition Width="5" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="3*" />
				<ColumnDefinition Width="5*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<Label Grid.Column="0"
				   Content="年" />
			<TextBox Grid.Column="1"
					 MaxLength="4"
					 Text="{Binding SearchOpt.SelYear}"/>
			<Label Grid.Column="3"
				   Content="基準週" />
			<ComboBox Grid.Column="4" 
					  VerticalAlignment="Top"  
					  VerticalContentAlignment="Center" 
					  ItemsSource="{Binding BaseWeekList}"
					  DisplayMemberPath="Value"
					  SelectedValuePath="Key" 
					  SelectedValue="{Binding SearchOpt.SelBaseWeek}"
					  Style="{StaticResource SearchableComboBox1}"/>
			<Button Grid.Column="6"
					Width="80"
					VerticalAlignment="Center"
					HorizontalAlignment="Right"
					Command="{Binding DoSearchCommand}"
					Content="表示更新(F5)" />
		</Grid>

		<uc:CvnetFlexView Grid.Row="1" ItemsSource="{Binding WeekList}">
			<uc:CvnetFlexView.Columns>
				<DataGridTextColumn Header="年" Binding="{Binding Year}" Width="*" />
				<DataGridTextColumn Header="週NO" Binding="{Binding WeekNo}" Width="*" />
				<DataGridTemplateColumn Header="開始日" Width="*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<DatePicker SelectedDate="{Binding StartDate}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTextColumn Header="曜" Binding="{Binding StartDayWeek}" Width="Auto" />
				<DataGridTemplateColumn Header="終了日" Width="*">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<DatePicker SelectedDate="{Binding EndDate}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTextColumn Header="曜" Binding="{Binding EndDayWeek}" Width="Auto" />
				<DataGridTextColumn Header="メモ" Binding="{Binding Memo}" Width="4*" />
			</uc:CvnetFlexView.Columns>
		</uc:CvnetFlexView>

		<Grid Grid.Row="2" Margin="5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<Label Grid.Column="0"
				   Content="※年・週NOが赤の場合、該当年が登録されていませんので更新実行してください"
				   Foreground="Red" />
			<Button Grid.Column="1"
					Margin="0,0,5,0"
					Width="80"
					Command="{Binding DoDeleteCommand}"
					Content="削除実行" />
			<Button Grid.Column="2"
					Margin="0,0,5,0"
					Width="80"
					Command="{Binding DoExecuteCommand}"
					Content="更新実行" />
			<Button Grid.Column="3"
					Margin="0,0,5,0"
					Width="80"
					Command="{Binding DoExitCommand}"
					Content="戻る(ESC)" />
		</Grid>

		<Grid Grid.Row="3" Margin="5">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="5*" />
				<ColumnDefinition Width="5*" />
			</Grid.ColumnDefinitions>
			<Label Grid.Column="1" 
				   Foreground="Red"
				   Content="{Binding Mess2}" />
		</Grid>
	</Grid>
</Window>
