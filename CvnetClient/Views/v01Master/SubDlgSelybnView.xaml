<Window x:Class="CvnetClient.Views.SubDlgSelybnView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        mc:Ignorable="d" 
		Title="{Binding Title}"
        Style="{StaticResource MasterWindowStyle}"
        WindowStartupLocation="CenterScreen">
	<Window.DataContext>
		<vm:SubDlgSelybnViewModel />
	</Window.DataContext>
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="5" />
		</Grid.RowDefinitions>

		<DataGrid Grid.Row="0"
				  AutoGenerateColumns="False"
				  CanUserAddRows="False"
				  HeadersVisibility="Column"
				  Margin="5"
				  ItemsSource="{Binding SelybnFlexList}"
				  SelectedValue="{Binding SelectedSelybn}"
				  Style="{DynamicResource WinGridData}">
			<DataGrid.Columns> 
				<DataGridTextColumn Header="〒" Binding="{Binding Col01}" Width="*"/>
                <DataGridTextColumn Header="住所1" Binding="{Binding Col02}" Width="*"/>
                <DataGridTextColumn Header="住所2" Binding="{Binding Col03}" Width="*"/>
            </DataGrid.Columns>
		</DataGrid>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="0.5*" />
				<ColumnDefinition Width="0.5*" />
				<ColumnDefinition Width="6*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<Button Grid.Column="0" 
					Content="&lt;&lt;"
					Margin="5,5,0,5"
					Command="{Binding PrevListCommand}"/>
			<Button Grid.Column="1" 
					Content="&gt;&gt;" 
					Margin="5,5,0,5"
					Command="{Binding NextListCommand}"/>

			<Button Grid.Column="3" 
					Command="{Binding DoSearchCommand}"
					Style="{DynamicResource ExecButton}" 
					Margin="5,5,0,5"/>
			<Button Grid.Column="4" 
					Command="{Binding DoExitCommand}"
					Style="{DynamicResource BackBtn}" 
					Margin="5,5,0,5"/>

		</Grid>
		
    </Grid>
</Window>
