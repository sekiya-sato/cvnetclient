<Window x:Class="CvnetClient.Views.SubDlg05PrnSwatchsdView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CvnetClient.Views"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        xmlns:utils="clr-namespace:CvnetClient.Utils"
        xmlns:uc="clr-namespace:CvnetClient.Views"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="展示会SD入力結果出力" Style="{StaticResource SmallWindow}">
    <Window.DataContext>
        <vm:SubDlg05PrnSwatchsdViewModel/>
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>

            <Label Content="展示会" Grid.Column="0"/>
            <TextBox Grid.Column="1" Text="{Binding Condition.Exhibition}"/>


            <Label  Grid.Row="2" Grid.Column="0" Content="お客様CD"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                <TextBox MinWidth="200" TextWrapping="Wrap" Text="{Binding Condition.CustCD.Display}"/>
                <uc:CvnetBtListView MstName="全得意先" StyleDesign="2" 
                        SelectedValue ="{Binding Condition.CustCD.Code}"
                        Command="{Binding SelCustCommand}"/>
            </StackPanel>
            
        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="1"
            HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button Style="{DynamicResource ExecButton}" Margin="0,0,8,0" Command="{Binding DoPrintCommand}"/>
            <Button Style="{DynamicResource BackBtn}" Command="{Binding ExitCommand}"/>
        </StackPanel>
    </Grid>
</Window>
