<Window x:Class="CvnetClient.Views.SubDlg09UptounyuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CvnetClient.Views"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        xmlns:utils="clr-namespace:CvnetClient.Utils"
        xmlns:uc="clr-namespace:CvnetClient.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="SubDlg90Jidou08View" Height="350" Width="650">
    <Window.DataContext>
        <vm:SubDlg09UptounyuViewModel/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding ExitCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Text="※初期在庫から投入数を作成します" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Label Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Content="更新対象年月:"/>
            <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding Status}" IsReadOnly="True" Height="30" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="LightGray"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="実行中">
                                <Setter Property="Background" Value="LightGray"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding Status}" Value="停止中">
                                <Setter Property="Background" Value="Red"/> 
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            
            
            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="5" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Label VerticalAlignment="Center" HorizontalAlignment="Right" Content="倉庫"/>
                <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBox Width="auto" MinWidth="170" TextWrapping="Wrap">
                        <TextBox.Text>
                            <MultiBinding StringFormat='{}{0} {1}'>
                                <Binding Path="FindWarehouse1.Code" />
                                <Binding Path="FindWarehouse1.Name" />
                            </MultiBinding>
                        </TextBox.Text>
                    </TextBox>
                    <uc:CvnetBtListView MstName="移動倉庫" StyleDesign="2" 
SelectedValue ="{Binding FindWarehouse1.Code}"
Command="{Binding SelWareHouse1Command}"/>
                </StackPanel>
                <Label Content="~" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal"  VerticalAlignment="Center" HorizontalAlignment="Left">
                    <TextBox Width="auto" MinWidth="170" TextWrapping="Wrap">
                        <TextBox.Text>
                            <MultiBinding StringFormat='{}{0} {1}'>
                                <Binding Path="FindWarehouse2.Code" />
                                <Binding Path="FindWarehouse2.Name" />
                            </MultiBinding>
                        </TextBox.Text>
                    </TextBox>
                    <uc:CvnetBtListView MstName="移動倉庫" StyleDesign="2" 
SelectedValue ="{Binding FindWarehouse2.Code}"
Command="{Binding SelWareHouse2Command}"/>
                </StackPanel>
            </StackPanel>
            

            <Button Grid.Row="3" Grid.Column="1" Style="{StaticResource ExecButton}" HorizontalAlignment="Center" Command="{Binding DoRunCommand}" Content="実行(F5)"/>
            <Button Grid.Row="3" Grid.Column="3" Style="{StaticResource BackBtn}"    HorizontalAlignment="Center" Command="{Binding ExitCommand}"/>
        </Grid>
        

    </Grid>
</Window>
