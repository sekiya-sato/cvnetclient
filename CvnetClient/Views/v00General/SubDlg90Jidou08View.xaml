<Window x:Class="CvnetClient.Views.SubDlg90Jidou08View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CvnetClient.Views"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        xmlns:utils="clr-namespace:CvnetClient.Utils"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="SubDlg90Jidou08View" Style="{StaticResource SmallWindow}">
    <Window.DataContext>
        <vm:SubDlg90Jidou08ViewModel/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding ExitCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Content="状態:"/>
            <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding Status}" IsReadOnly="True" Height="30" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="LightGray"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="実行中">
                                <Setter Property="Background" Value="LightGray"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding Status}" Value="停止中">
                                <Setter Property="Background" Value="Red"/> 
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <TextBlock Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Text="自動起動処理の実行・停止を行います" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <Button Grid.Row="3" Grid.Column="1" Style="{StaticResource ExecButton}" HorizontalAlignment="Center" Command="{Binding DoRunCommand}"/>
            <Button Grid.Row="3" Grid.Column="2" Style="{StaticResource DeleteIconBtn3}" HorizontalAlignment="Center" Command="{Binding DoStopCommand}"/>
            <Button Grid.Row="3" Grid.Column="3" Style="{StaticResource BackBtn}" HorizontalAlignment="Center" Command="{Binding ExitCommand}"/>
        </Grid>
        

    </Grid>
</Window>
