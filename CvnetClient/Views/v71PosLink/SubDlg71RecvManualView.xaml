<Window x:Class="CvnetClient.Views.SubDlg71RecvManualView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CvnetClient.Views.v71PosLink"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:uc="clr-namespace:CvnetClient.Views"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        xmlns:utils="clr-namespace:CvnetClient.Utils"
        mc:Ignorable="d"
        Title="SubDlg71RecvManualView"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource PrintWindowStyle}">
    <Window.DataContext>
        <vm:SubDlg71RecvManualViewModel/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Key="Return" Command="{Binding DoListCommand}" />
        <KeyBinding Key="Escape" Command="{Binding ExitCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="1" Content="受信ログ" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>
        <TextBox
            MinHeight="250"
            Grid.Row="1"
            Grid.Column="1"
            IsReadOnly="True"
            Text="{Binding Text2}"
            FontSize="13"
            BorderBrush="#CBD5E1"
            BorderThickness="1"
            Background="#F9FAFB"
            VerticalAlignment="Top"
            VerticalContentAlignment="Top"
            HorizontalContentAlignment="Left"/>
        <TextBlock
            Width="250"
            Height="70"
            Grid.Row="2"
            Grid.Column="1"
            VerticalAlignment="Center" 
            HorizontalAlignment="Left"
            Text="{Binding Label4}"/>
        <Button
            Grid.Row="2"
            Grid.Column="1"
            VerticalAlignment="Center" 
            HorizontalAlignment="Right"
            Margin="0,0,150,0"
            Style="{StaticResource ExecButton}"
            Width="150"
            Content="データ作成(Enter)"
            Command="{Binding DoListCommand}"/>
        <Button
            Grid.Row="2"
            Grid.Column="1"
            VerticalAlignment="Center" 
            HorizontalAlignment="Right"
            Style="{StaticResource BackBtn}"
            Command="{Binding ExitCommand}" 
            IsCancel="True"/>
    </Grid>
</Window>
