<Window x:Class="CvnetClient.Views.SubDlgSelShoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
		xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
		xmlns:uc="clr-namespace:CvnetClient.Views"
		xmlns:vm="clr-namespace:CvnetClient.ViewModels" 
        Title="検索条件" Height="600" Width="900" 
        Style="{StaticResource MasterWindowStyle}">
	<Window.DataContext>
		<vm:SubDlgSelShoViewModel />
	</Window.DataContext>
	<behaviors:Interaction.Triggers>
		<behaviors:EventTrigger EventName="ContentRendered">
			<behaviors:InvokeCommandAction Command="{Binding InitCommand}" />
		</behaviors:EventTrigger>
	</behaviors:Interaction.Triggers>
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="6*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0" 
				   Text="検索条件"  
				   VerticalAlignment="Center"
                   Margin="0 0 0 5"/>

		<!-- 検索条件 -->
		<Border Grid.Row="1" 
				CornerRadius="10" BorderThickness="1" 
				Padding="3" Margin="3"
				Background="White">

			<StackPanel>
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="9*" />
					</Grid.ColumnDefinitions>

					<!-- 商品CD & 商品名 -->
					<Button Grid.Row="0" Grid.Column="0"
							Margin="5"
							Content=" 商品CD "  
							VerticalAlignment="Center"/>
					<Grid Grid.Row="0" Grid.Column="1"
						  Margin="5">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<TextBox Grid.Column="0" />
						<TextBlock Grid.Column="1" 
								   Text="～" 
								   VerticalAlignment="Center"
								   Margin="5, 0, 5, 0"/>
						<TextBox Grid.Column="2" />
					</Grid>

					<TextBlock Grid.Row="1" Grid.Column="0"
							   Margin="5"
							   Text="商品名" 
							   VerticalAlignment="Center"/>
					<Grid Grid.Row="1" Grid.Column="1" 
						  Margin="5">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="2*" />
							<ColumnDefinition Width="2*" />
							<ColumnDefinition Width="5*" />
						</Grid.ColumnDefinitions>

						<CheckBox Grid.Column="0" 
								  Content="B/S" 
								  Margin="0, 0, 10, 0"
								  VerticalAlignment="Center"/>

						<ComboBox Grid.Column="1"  
								  Margin="0, 0, 5, 0" 
								  VerticalContentAlignment="Center" 
								  DisplayMemberPath="Value" 
								  SelectedValuePath="Key"
								  Style="{StaticResource SearchableComboBox1}"> 
						</ComboBox>

						<ComboBox Grid.Column="2"  
								  Margin="0, 0, 5, 0" 
								  VerticalContentAlignment="Center" 
								  DisplayMemberPath="Value" 
								  SelectedValuePath="Key"
								  Style="{StaticResource SearchableComboBox1}">
						</ComboBox>

						<TextBox Grid.Column="3"
								 VerticalAlignment="Center"/> 
					</Grid> 

					<!-- 日付 + CheckBox -->
					<TextBlock Grid.Row="2" Grid.Column="0" 
							   Margin="5"
							   Text="店頭投入日" 
							   VerticalAlignment="Center"/>
					<Grid Grid.Row="2" Grid.Column="1"
						  Margin="5">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="1.5*" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="3*" />
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition Width="3*" />
						</Grid.ColumnDefinitions>

						<DatePicker Grid.Column="0" 
									Margin="0, 0, 10, 0"
									VerticalContentAlignment="Center" />

						<TextBlock Grid.Column="1"  
								   Text="在庫"
								   Margin="0, 0, 5, 0"
								   VerticalAlignment="Center" />

						<CheckBox Grid.Column="2" 
								  Content="使用" 
								  Margin="0, 0, 5, 0"
								  VerticalAlignment="Center"/>

						<TextBox Grid.Column="3" />

						<TextBlock Grid.Column="4" 
								   Text="～" 
								   VerticalAlignment="Center"
								   Margin="5, 0, 5, 0"/>

						<TextBox Grid.Column="5"  />

					</Grid>

					<!-- 条件結合 -->
					<TextBlock Grid.Row="3" Grid.Column="0"
							   Margin="5"
							   Text="条件結合" 
							   VerticalAlignment="Center"/>
					<StackPanel Grid.Row="3" Grid.Column="1" 
								Orientation="Horizontal"
								Margin="5">
						<RadioButton Content="AND" 
									 Margin="0,0,10,0"
									 IsChecked="True"/>
						<RadioButton Content="OR"/>
					</StackPanel>
				</Grid>
			</StackPanel>
		</Border>

		<!-- 条件リスト DataGrid -->
		<uc:ListFlexView Grid.Row="2" 
						 Config="{Binding ListConfig}"
						 Rows="{Binding ListFlex}" 
						 QueryString="{Binding WhereClaus, Mode=TwoWay}" Loaded="ListFlexView_Loaded" />

		<!-- Action Buttons -->
		<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" > 
			<Button Style="{DynamicResource ExecButton}" Grid.Column="4" Margin="0, 0, 5, 0" />
			<Button Style="{DynamicResource BackBtn}" Grid.Column="4" Margin="0, 0, 5, 0" />
		</StackPanel>
	</Grid>
</Window>
