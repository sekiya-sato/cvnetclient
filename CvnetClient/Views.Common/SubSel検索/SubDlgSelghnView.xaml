<Window x:Class="CvnetClient.Views.SubDlgSelghnView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CvnetClient.ViewModels"
		xmlns:util="clr-namespace:CvnetClient.Utils"
        mc:Ignorable="d"
        Title="{Binding DgTitle}" 
		Height="600" Width="600"
		Style="{StaticResource MasterWindowStyle}">
	<Window.DataContext>
		<vm:SubDlgSelghnViewModel />
	</Window.DataContext>
	<Window.Resources>
		<util:DateDisplayConverter x:Key="DateDisplayConverter" /> 
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="10" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="10" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="10" />
		</Grid.ColumnDefinitions>
		<DataGrid Grid.Row="0" Grid.Column="1"
				  d:ItemsSource="{d:SampleData ItemCount= 5}"
				  IsReadOnly="True" 
				  ColumnWidth="*" 
				  HeadersVisibility="Column"
				  AutoGenerateColumns="False"
				  IsSynchronizedWithCurrentItem="True"
				  ItemsSource="{Binding SelghnList}"
				  Style="{StaticResource WinGridData}"
				  SelectionMode="Single">
			<DataGrid.Columns>
				<DataGridTextColumn Width="*" Binding="{Binding ChangeDate, Converter={StaticResource DateDisplayConverter} }" Header="変更日" />
				<DataGridTextColumn Width="*" Binding="{Binding OriCost}" Header="原価">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="TextAlignment" Value="Right" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTextColumn Width="*" Binding="{Binding PurchSlipNo}" Header="仕入伝票NO">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="TextAlignment" Value="Right" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
			</DataGrid.Columns>
		</DataGrid>
		<StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right">
			<Button Content=" 戻る(ESC) " 
					Command="{Binding DoExitCommand}"
					Margin="0,5,0,0"/> 
		</StackPanel>
	</Grid>
</Window>
