<Window x:Class="CvnetClient.Views.SubDlgSel10View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:vm="clr-namespace:CvnetClient.ViewModels"
        mc:Ignorable="d" 
		Title="{Binding Title}"
        Style="{StaticResource MasterWindowStyle}">
	<Window.DataContext>
		<vm:SubDlgSel10ViewModel />
	</Window.DataContext>
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="5" />
		</Grid.RowDefinitions>

		<DataGrid Grid.Row="0"
				  AutoGenerateColumns="False"
				  CanUserAddRows="False"
				  HeadersVisibility="Column"
				  Margin="5"
				  ItemsSource="{Binding Sel01FlexList}"
				  SelectedValue="{Binding SelectedSel10}"
				  Style="{DynamicResource WinGridData}">
			<DataGrid.Columns> 
				<DataGridTextColumn Header="コード" Binding="{Binding Col01}" Width="*"/> 
				<DataGridTextColumn Header="名" Binding="{Binding Col02}" Width="*"/>
				<DataGridTextColumn Header="TEL" Binding="{Binding Col03}" Width="*"/>
				<DataGridTextColumn Header="〒" Binding="{Binding Col04}" Width="*"/>
				<DataGridTextColumn Header="住所" Binding="{Binding Col05}" Width="*"/>
				<DataGridTextColumn Header="取引金額" Binding="{Binding Col06}" Width="*"/>
				<DataGridTextColumn Header="取引数" Binding="{Binding Col07}" Width="0.5*"/> 
			</DataGrid.Columns>
		</DataGrid>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="0.5*" />
				<ColumnDefinition Width="0.5*" />
				<ColumnDefinition Width="6*" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>

			<Button Grid.Column="0" 
					Content="&lt;&lt;"
					Margin="5,5,0,5"
					Command="{Binding PrevListCommand}"/>
			<Button Grid.Column="1" 
					Content="&gt;&gt;" 
					Margin="5,5,0,5"
					Command="{Binding NextListCommand}"/>

			<Button Grid.Column="3" 
					Command="{Binding DoSearchCommand}"
					Style="{DynamicResource ExecButton}" 
					Margin="5,5,0,5"/>
			<Button Grid.Column="4" 
					Command="{Binding DoExitCommand}"
					Style="{DynamicResource BackBtn}" 
					Margin="5,5,0,5"/>

		</Grid>
		
    </Grid>
</Window>
