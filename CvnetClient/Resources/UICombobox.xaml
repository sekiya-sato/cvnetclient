<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:util="clr-namespace:CvnetClient.Utils">
	
	<util:SubtractConverter x:Key="SubtractConverter" />
	
	<!--Searchable Combobox Template 1-->
	<!-- Toggle Button for ComboBox -->
	<ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
		<Border Background="Black"
            BorderBrush="Black"
            BorderThickness="0,0,1,0" Padding="2">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<!-- Content Presenter (for text) -->
				<ContentPresenter Grid.Column="0"
                          Margin="5,0,0,0"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Left" />
				<!-- Drop-down Arrow -->
				<Border Grid.Column="1"
                Width="Auto"
                Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                Background="Transparent"
                HorizontalAlignment="Right"
                VerticalAlignment="Center">
					<Path Data="M 0 0 L 4 4 L 8 0 Z"
                Fill="White"
                Width="8"
                Height="6"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>
				</Border>
			</Grid>
		</Border>
	</ControlTemplate>

	<!-- TextBox Template for Editable ComboBox -->
	<ControlTemplate x:Key="ComboBoxTextBox" TargetType="{x:Type TextBox}">
		<Border BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="0"
            Background="{TemplateBinding Background}">
			<ScrollViewer x:Name="PART_ContentHost" />
		</Border>
	</ControlTemplate>

	<!-- Searchable ComboBox Style -->
	<Style x:Key="SearchableComboBox1" TargetType="{x:Type ComboBox}">
		<Setter Property="SnapsToDevicePixels" Value="True" />
		<Setter Property="OverridesDefaultStyle" Value="True" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="IsEditable" Value="True" />
		<Setter Property="MinHeight" Value="25" />
		<Setter Property="BorderBrush" Value="Black" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type ComboBox}">
					<Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
						<Grid>
							<!-- Define 2 Columns: One for TextBox and one for the Arrow -->
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<!-- TextBox Area -->
								<ColumnDefinition Width="Auto" />
								<!-- Arrow Area -->
							</Grid.ColumnDefinitions>

							<!-- Editable TextBox -->
							<TextBox x:Name="PART_EditableTextBox"
                       Style="{x:Null}"
                       Template="{StaticResource ComboBoxTextBox}"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Center"
					   VerticalContentAlignment="Center"
					   HorizontalContentAlignment="Left"
                       Margin="1" 
                       Focusable="True"
                       IsReadOnly="{Binding Path=IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}"
                       Text="{TemplateBinding Text}"
                       Grid.Column="0"
                       Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}, Converter={StaticResource SubtractConverter}, ConverterParameter=20}"
					   Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}">
								<!-- TextBox Width Adjustment -->
							</TextBox>

							<!-- Drop-down Arrow Button -->
							<ToggleButton x:Name="ToggleButton"
                            Template="{StaticResource ComboBoxToggleButton}"
                            Grid.Column="1"
                            Focusable="false"
                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            ClickMode="Press"
                            Width="20" Height="{TemplateBinding Height}" />
							<!-- Adjust Arrow Button Width -->

							<!-- Drop-down Popup -->
							<Popup x:Name="Popup"
                     Placement="Bottom"
                     IsOpen="{TemplateBinding IsDropDownOpen}"
                     AllowsTransparency="True"
                     Focusable="False"
                     PopupAnimation="Slide"
                     Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}">
								<Grid MaxHeight="200" Width="{TemplateBinding ActualWidth}">
									<Border x:Name="DropDownBorder"
                          BorderThickness="1"
                          BorderBrush="Gray"
                          Background="White">
										<ScrollViewer Margin="4" SnapsToDevicePixels="True">
											<ItemsPresenter />
										</ScrollViewer>
									</Border>
								</Grid>
							</Popup>
						</Grid>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="DropDownBorder" Property="BorderBrush" Value="Gray" />
						</Trigger>
						<Trigger Property="IsDropDownOpen" Value="True">
							<Setter Property="KeyboardNavigation.IsTabStop" Value="False" />
						</Trigger>
						<Trigger Property="HasItems" Value="False">
							<Setter TargetName="DropDownBorder" Property="MinHeight" Value="95" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
	<!--Searchable Combobox Template 1-->
</ResourceDictionary>